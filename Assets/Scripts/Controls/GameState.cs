using UnityEngine;

public class GameState : MonoBehaviour
{
    public CardSelector cardSelectorScript;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
